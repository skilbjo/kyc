// app/controllers/users.js// need to ask Bernhard about this code below...// also need to ask how to get the model in here.var models = {    users       : require('../models/user.js'),    business    : require('../models/business.js'),    reviews     : require('../models/review.js')};// app.get('/auth/facebook', controllers.users.authFacebook(app, passport) );// passport = require('passport');// require('../../config/passport')(passport); // pass passport for configuration// exports.authFacebook = function(req, res) {//   passport.authenticate('facebook', { scope : 'email' });// };// exports.getProfile = function(isLoggedIn, req, res) {//         models.users.find({}, function(err, users) {//            res.render('profile.hbs', {//                 user            : req.user,//                 users           : users//            });   //         });   // };// exports.authFacebook = function(app, passport) {//   passport.authenticate('facebook', { scope : 'email' });// };// Controller actionsexports.index = function (req, res) {  res.render('index.hbs');};exports.signup = function (req, res) {  res.render('signup.hbs', { message: req.flash('loginMessage') })};exports.login = function (req, res) {  res.render('login.hbs', { message: req.flash('loginMessage') });};exports.logout = function (req, res) {  req.logout;  res.redirect('/?logout=true');};exports.edit = function (req, res, phone, streetAddress, cityAddress, stateAddress) {      var user = req.params.id;      res.render('/profile/edit.hbs', {        title: 'Editing user ' + req.user.name,        user: req.user      });    });};exports.editSave = function (req, res, phone, streetAddress, cityAddress, stateAddress) {  process.nextTick( function() {    var user            = req.user;      user.local.phone            = phone;      user.local.streetAddress    = streetAddress;      user.local.cityAddress      = cityAddress;      user.local.stateAddress     = stateAddress;      user.save(function(err) {          if (err)              throw err;          return done(null, user);      });      var user = req.params.id;      res.render('/profile.hbs', { message: req.flash('Profile Saved!') });      });    });};// route middleware to ensure user is logged in// function isLoggedIn(req, res, next) {//         if (req.isAuthenticated())//                 return next();//         res.redirect('/');// }